% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chi_dev_to_prod.R
\name{chi_dev_to_prod}
\alias{chi_dev_to_prod}
\title{CHI Dev to Prod}
\usage{
chi_dev_to_prod(
  table_name = NULL,
  exclude_keys = NULL,
  exact_match = FALSE,
  confirm = TRUE
)
}
\arguments{
\item{table_name}{character. The root name of the SQL table to transfer,
e.g., \code{'birth'}, \code{'brfss'}, \code{'death'}. The function operates on
\verb{[table_name]_results} and \verb{[table_name]_metadata} in both dev and prod.}

\item{exclude_keys}{character vector or \code{NULL}. One or more strings used to
identify \code{indicator_key} values that should \strong{NOT} be copied from dev to
prod. Matching behavior is controlled by \code{exact_match}.

Default \code{exclude_keys = NULL}; meaning nothing is excluded, i.e., all dev
\code{indicator_key} values are copied to prod.}

\item{exact_match}{logical. Controls how \code{exclude_keys} are applied.
\itemize{
\item \code{FALSE}: each string in \code{exclude_keys} is matched as a
substring pattern using base R's \code{\link[base:grep]{base::grepl()}}. For example,
\code{exclude_keys = 'uninsure'} will exclude any \code{indicator_key} containing
the text \code{'uninsure'} (e.g., \code{'uninsured_adult'}, \code{'uninsured_child'}).
\item \code{TRUE}: each string in \code{exclude_keys} must match an \code{indicator_key}
exactly. Use this when indicator keys do not share a common root.
}

Default \code{exact_match = FALSE}.}

\item{confirm}{logical. If \code{TRUE}, the user is shown a summary table of
\code{indicator_key} values and their dev row counts before any writing to prod.
The user must type \strong{\code{y}} to continue. Set to \code{FALSE} to
skip the prompt.

Default \code{confirm = TRUE}.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effects: copying data
from dev to prod.
}
\description{
Transfers CHI results and metadata from the development SQL Server
(\code{KCITSQLUATHIP40}, schema \code{APDE_WIP}) to the production SQL Server
(\code{KCITSQLPRPHIP40}, schema \code{APDE}). Because no linked server exists between
dev and prod, matching rows are pulled from dev into R and then written to
prod.
}
\details{
For each \code{indicator_key} that is \strong{not} excluded (via \code{exclude_keys}), the
function:
\enumerate{
\item Opens connections to both the dev and prod SQL Servers.
\item Queries dev for all \code{indicator_key} values present in the results table,
then applies \code{exclude_keys} filtering to determine which keys will be
transferred.
\item Optionally shows the user a confirmation table (row counts per
\code{indicator_key} from dev) and waits for approval before writing to prod.
\item Pulls matching rows from dev into R.
\item Deletes those \code{indicator_key} rows from the corresponding prod tables.
\item Appends the pulled rows to prod.
\item Runs a post-copying row count check to confirm success.
}

\code{indicator_key} values identified by \code{exclude_keys} are skipped entirely.
They are neither deleted from prod nor copied from dev. All other rows
already in prod for those excluded keys are left as-is.

For details on server access and local configuration, please review:
SharePoint > DPH-KCCross-SectorData > Documents > References > SQL >
SQL Server Setup APDE.docx.
}
\examples{
\dontrun{
# Transfer all birth indicators from dev to prod
chi_dev_to_prod(
  table_name = "birth"
)

# Transfer acs indicators, excluding indicator_keys starting with 'uninsured'
chi_dev_to_prod(
  table_name   = "acs",
  exclude_keys = "^uninsure",
  exact_match  = FALSE
)

# Exclude two specific indicator_key values by exact name, skip confirmation
chi_dev_to_prod(
  table_name   = "acs",
  exclude_keys = c("uninsured_adult", "uninsured_child"),
  exact_match  = TRUE,
  confirm      = FALSE
)
}

}
\seealso{
\code{\link[=chi_update_sql]{chi_update_sql()}} for pushing data from R to either the dev or prod server.

\code{\link[=chi_qa_tro]{chi_qa_tro()}} for validating data before upload.
}
\keyword{CHI,}
\keyword{Production,}
\keyword{SQL}
\keyword{Tableau,}
